"use strict";
const auth      = require('./auth'),
      messaging = require('./messaging'),
      chatting  = require('./chatting');

module.exports = function attachEvents(socket) {
  auth(socket);
  messaging(socket);
  chatting(socket);
  return socket;
};