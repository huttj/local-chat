'use strict';
module.exports = function(eventsObject){
  return function(socket) {
    for (let key in eventsObject) {
      socket.on(key, eventsObject[key].bind(socket));
    }
    return socket;
  }
};